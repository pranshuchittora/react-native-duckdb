///
/// HybridPreparedStatementSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `HybridQueryResultSpec` to properly resolve imports.
namespace margelo::nitro::rnduckdb { class HybridQueryResultSpec; }

#include <memory>
#include "HybridQueryResultSpec.hpp"
#include <NitroModules/Null.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <string>
#include <variant>
#include <vector>
#include <optional>
#include <NitroModules/Promise.hpp>
#include <unordered_map>

namespace margelo::nitro::rnduckdb {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `PreparedStatement`
   * Inherit this class to create instances of `HybridPreparedStatementSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPreparedStatement: public HybridPreparedStatementSpec {
   * public:
   *   HybridPreparedStatement(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPreparedStatementSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPreparedStatementSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPreparedStatementSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<HybridQueryResultSpec> executeSync(const std::optional<std::vector<std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>>>& params) = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<HybridQueryResultSpec>>> execute(const std::optional<std::vector<std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>>>& params) = 0;
      virtual std::shared_ptr<HybridQueryResultSpec> executeSyncNamed(const std::unordered_map<std::string, std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>>& params) = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<HybridQueryResultSpec>>> executeNamed(const std::unordered_map<std::string, std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>>& params) = 0;
      virtual void finalize() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "PreparedStatement";
  };

} // namespace margelo::nitro::rnduckdb
