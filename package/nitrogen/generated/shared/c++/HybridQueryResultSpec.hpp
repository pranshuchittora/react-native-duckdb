///
/// HybridQueryResultSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <vector>
#include <NitroModules/Null.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <variant>
#include <unordered_map>

namespace margelo::nitro::rnduckdb {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `QueryResult`
   * Inherit this class to create instances of `HybridQueryResultSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridQueryResult: public HybridQueryResultSpec {
   * public:
   *   HybridQueryResult(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridQueryResultSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridQueryResultSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridQueryResultSpec() override = default;

    public:
      // Properties
      virtual double getRowCount() = 0;
      virtual double getRowsChanged() = 0;
      virtual double getColumnCount() = 0;
      virtual std::vector<std::string> getColumnNames() = 0;
      virtual std::vector<std::string> getColumnTypes() = 0;

    public:
      // Methods
      virtual std::vector<std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>> getColumn(double index) = 0;
      virtual std::vector<std::unordered_map<std::string, std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>>> toRows() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "QueryResult";
  };

} // namespace margelo::nitro::rnduckdb
