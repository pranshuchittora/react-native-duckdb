///
/// HybridAppenderSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <NitroModules/Null.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <string>
#include <variant>
#include <vector>

namespace margelo::nitro::rnduckdb {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Appender`
   * Inherit this class to create instances of `HybridAppenderSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridAppender: public HybridAppenderSpec {
   * public:
   *   HybridAppender(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridAppenderSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridAppenderSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridAppenderSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void appendRow(const std::vector<std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>>& values) = 0;
      virtual void appendRows(const std::vector<std::vector<std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>>>& rows) = 0;
      virtual void appendColumns(const std::vector<std::vector<std::variant<nitro::NullType, bool, int64_t, std::shared_ptr<ArrayBuffer>, std::string, double>>>& columns) = 0;
      virtual void flush() = 0;
      virtual void close() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Appender";
  };

} // namespace margelo::nitro::rnduckdb
